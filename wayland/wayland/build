#!/bin/sh -e

export DESTDIR="$1"

meson \
    --prefix=/usr \
    -Ddefault_library=both \
    -Dbuildtype=release \
    -Ddocumentation=false \
    -Ddtd_validation=false \
    . build

ninja -C build
ninja -C build install
