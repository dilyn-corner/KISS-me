#!/bin/sh -e

mv config .config
#gmake defconfig
LDFLAGS="$LDFLAGS --static" gmake
gmake PREFIX="$1/usr/bin" install_flat

#install -Dm755 toybox "$1/usr/bin/toybox"
#
#./toybox | sed 's/ /\n/g' | while read -r bin; do
#    ln -s toybox "$1/usr/bin/$bin"
#done
